on:
  pull_request:
    branches: [main]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run installation
        run: npm install
      - name: Run tests
        run: npm run test